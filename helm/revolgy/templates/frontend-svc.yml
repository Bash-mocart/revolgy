apiVersion: v1
kind: Service
metadata:  
  name: localhost-frontend
spec:
  selector:    
    app: frontend-label
  type: NodePort
  ports:  
  - name: http
    port: 80
    targetPort: 80
    nodePort: 30035
    protocol: TCP
