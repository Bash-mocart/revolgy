apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "revolgy.fullname" . }}-test-connection"
  labels:
    {{- include "revolgy.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "revolgy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
